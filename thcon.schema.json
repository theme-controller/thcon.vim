{
    "title": "thcon.vim payload",
    "description": "Interface for interacting with thcon.vim via thcon-listen",
    "type": "object",
    "properties": {
        "rc_file": {
            "type": "string",
            "required": true,
            "$comment": "The absolute path to the file to be `:source`d."
        }
    },
    "examples": [
        { "rc_file": "/home/alice/.local/share/thcon/vimrc" },
        { "rc_file": "/etc/vimrc" }
    ]
}
